# CloudKit JS browser configuration. The token is public client configuration,
# but should be restricted to the deployed web origin in CloudKit Console.
NEXT_PUBLIC_CLOUDKIT_CONTAINER_IDENTIFIER=iCloud.com.example.OurFinances
NEXT_PUBLIC_CLOUDKIT_API_TOKEN=replace-with-domain-restricted-browser-token
NEXT_PUBLIC_CLOUDKIT_ENVIRONMENT=development
