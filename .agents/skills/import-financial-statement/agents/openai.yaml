interface:
  display_name: "Import Financial Statement"
  short_description: "Visually verify and reconcile statements"
  default_prompt: "Use $import-financial-statement to visually verify every source fact and create an exact, reconciled import bundle."
